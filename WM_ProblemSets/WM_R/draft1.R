p = .32  # given prediction
actual = 195/425 # proportion who took survey

sd(actual)
sqrt((p * (1 - p)/425)) # sd of the population/sample population
sqrt((actual * (1 - actual)/425)) # se of sample proportion

